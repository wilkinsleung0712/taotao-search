<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.taotao.search.mapper.ItemMapper">
	<select id="getAllItems" resultType="com.taotao.search.pojo.Item">
		select a.id as
		item_id,a.title as item_title,a.sell_point as item_sell_point,a.price
		as item_price ,a.image as item_image,b.name as item_category_name from
		tb_item a left join tb_item_cat b on a.cid = b.id
	</select>
</mapper>